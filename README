TODO:
* fix uci (fully support)
* improve search:
  - improve ordering heuristics during search
  - imporove positon evaluation heuristics
  - multi-threaded
* make a max-heap class for moves instead of std::vector (no need to sort etc.) (worth?)